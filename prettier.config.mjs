/** @type {import("prettier").Config} */
const config = {
    semi: true,
    singleQuote: false,
    tabWidth: 4,
    trailingComma: "es5",
    printWidth: 100,
    plugins: [],
};

export default config;
